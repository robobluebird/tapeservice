<h1>uploads queued for <b><%= @tape["name"] %></b></h1>
<a href="/tapes/<%= @tape["name"] %>">back</a>
<div class="bumped">
  <ol>
    <% @uploads.each do |upload| %>
      <li>
        <%= upload.split(/(?<!\|)-/).first.gsub("|-", "-") %>
      </li>
    <% end %>
  </ol>
</div>
